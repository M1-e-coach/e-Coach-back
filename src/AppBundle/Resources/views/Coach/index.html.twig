{% extends 'dashbord.html.twig' %}

{% block body %}
    <style>
        div#tableProgramme_length {
            display: none;
        }
    </style>
    {% if userRoles == "ROLE_SUPER_ADMIN" or userRoles == "ROLE_COACH" %}
        <nav class="blue-grey" style="padding:0 20px!important;height:50px;line-height:50px;opacity:0.5;">
            <div class="nav-wrapper right-align">
                <div class="col s12">
                    <a href="{{ path('app_accueil_index') }}" class="breadcrumb" style="font-size:15px;">Accueil</a>
                    <a href="{{ path('app_coachplanning_index') }}" class="breadcrumb" style="font-size:15px;">Coach</a>
                </div>
            </div>
        </nav>

        <h3 class="center-align">Liste de toutes mes disponibilités</h3>
        <div class="container">
            <div class="row">
                <div class="col s12 center-align">
                    {% if userRoles == "ROLE_COACH" %}
                        <a href="{{ path('app_coachplanning_add') }}" class="center-align customBlue waves-effect waves-light btn-large">
                            <i class="material-icons left">add</i>Ajouter une disponibilité
                        </a>
                    {% endif %}
                </div>
                <table class="striped mdl-data-table" id="tableProgramme">
                    <thead>
                    <tr>
                        <th>Seance</th>
                        <th>Date de début</th>
                        <th>Date de fin</th>
                        {% if userRoles == "ROLE_COACH" %}
                            <th>Actions</th>
                        {% endif %}
                    </tr>
                    </thead>

                    <tbody>
                    {% for coachPlanning in coachPlannings %}
                        <tr>
                            <td>{{ coachPlanning.seance.nom }}</td>
                            <td>{{ coachPlanning.datedebut|date('Y-m-d') }} - {{ coachPlanning.heuredebut|date('H:i:s') }}</td>
                            <td>{{ coachPlanning.datefin|date('Y-m-d') }} - {{ coachPlanning.heurefin|date('H:i:s') }}</td>
                            {% if userRoles == "ROLE_COACH" %}
                                <td>
                                    {#<a href="{{ path('app_programme_update', {'id': programme.id }) }}">
                                        <i class="fa fa-2x fa-pencil orange-text" aria-hidden="true"></i>
                                    </a>
                                    <a href="{{ path('app_programme_delete', {'id': programme.id }) }}">
                                        <i class="fa fa-2x fa-times red-text" aria-hidden="true"></i>
                                    </a>#}
                                </td>
                            {% endif %}
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    {% endif %}
    <script>
        {% if userRoles == "ROLE_COACH" %}
        var iconMenu = document.getElementById('users');
        iconMenu.style = "display:none;";
        {% endif %}
        {% if userRoles == "ROLE_SUPER_ADMIN" %}
        var iconMenuDispo = document.getElementById('dispos');
        iconMenuDispo.style = "display:none;";
        var iconMenuProgs = document.getElementById('progs');
        iconMenuProgs.style = "display:none;";
        {% endif %}
        $(document).ready(function() {
            $('#tableProgramme').DataTable( {
                columnDefs: [
                    {
                        targets: [ 0, 1, 2 ],
                        className: 'mdl-data-table__cell--non-numeric'
                    }
                ],
                language: {
                    "sProcessing":     "Traitement en cours...",
                    "sSearch":         "Rechercher&nbsp;:",
                    "sLengthMenu":     "Afficher _MENU_ &eacute;l&eacute;ments",
                    "sInfo":           "Affichage de l'&eacute;l&eacute;ment _START_ &agrave; _END_ sur _TOTAL_ &eacute;l&eacute;ments",
                    "sInfoEmpty":      "Affichage de l'&eacute;l&eacute;ment 0 &agrave; 0 sur 0 &eacute;l&eacute;ment",
                    "sInfoFiltered":   "(filtr&eacute; de _MAX_ &eacute;l&eacute;ments au total)",
                    "sInfoPostFix":    "",
                    "sLoadingRecords": "Chargement en cours...",
                    "sZeroRecords":    "Aucun &eacute;l&eacute;ment &agrave; afficher",
                    "sEmptyTable":     "Aucune donn&eacute;e disponible dans le tableau",
                    "oPaginate": {
                        "sFirst":      "Premier",
                        "sPrevious":   "Pr&eacute;c&eacute;dent",
                        "sNext":       "Suivant",
                        "sLast":       "Dernier"
                    },
                    "oAria": {
                        "sSortAscending":  ": activer pour trier la colonne par ordre croissant",
                        "sSortDescending": ": activer pour trier la colonne par ordre d&eacute;croissant"
                    },
                    "select": {
                        "rows": {
                            _: "%d lignes séléctionnées",
                            0: "Aucune ligne séléctionnée",
                            1: "1 ligne séléctionnée"
                        }
                    }
                }
            } );
        } );
    </script>
{% endblock %}